import os

#Makes a new folder or folders inside folder :-
# os.mkdir("data")
# if(not os.path.exists("data")):
#     os.mkdir("data")

#renames the files/folder , currently it's renaming folder names 
# for i in range(1,101):
#     os.rename(f"data/Day{i}" , f"data/tutorial{i}")


#prints folder and files inside the folder :-
# folders = os.listdir("data")
# for folders in folders:
#     print(folders)
#     print(os.listdir(f"data/{folders}"))

